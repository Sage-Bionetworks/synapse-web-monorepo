import { TextFieldProps } from '@mui/material/TextField'

/**
 * A @mui/material TextField component that a user can enter comma-separated values into.
 *
 * The component will store the entered state locally and propagate the value ot
 * @param props
 * @constructor
 */
export default function TextFieldCommaSeparatedInput(props: TextFieldProps) {}
