{
  // tsconfig.test.json defines the TypeScript rules we want to enforce for tests. This is currently only used by IDEs.
  "extends": "../tsconfig.json",
  "compilerOptions": {
    // Disable these strict TypeScript rules for mocks
    //    "noImplicitAny": false,
    //    "strictNullChecks": false
    "strict": false
  },
  "include": ["**/*", "**/*.json"],
  "references": [
    {
      "path": "../src"
    }
  ]
}
