{
  "extends": "../../shared/tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "rootDir": "./src",
    "outDir": "../../node_modules/.cache/.tsc_cache/synapse-react-client/build",
    "module": "esnext",
    "target": "esnext",
    "moduleResolution": "bundler",
    "allowJs": true,
    "noEmit": false,
    "isolatedModules": true,
    "noFallthroughCasesInSwitch": true,
    "paths": {
      "@/*": ["./src/*"],
      // HMR will work if we uncomment these, but doing so causes various build/typecheck errors
      //
      // In the markdown-it packages, the TypeScript compiler had an issue with the output type declaration file being
      // generated by tsup and not TypeScript. We could probably replace tsup with the TypeScript compiler, but that
      // might cause breaking changes that must be handled in SWC.
      //
      // In the synapse-client and synapse-types packages cause failures when parsed by the vite-plugin-dts plugin
      // that we use to generate a single type declaration file in synapse-react-client

      //  "@sage-bionetworks/synapse-client": ["../synapse-client/src"],
      //  "@sage-bionetworks/synapse-client/*": ["../synapse-client/src/*"],
      //  "@sage-bionetworks/synapse-types": ["../synapse-types/src"],
      //  "@sage-bionetworks/synapse-types/*": ["../synapse-types/src/*"],
      //  "markdown-it-synapse": ["../markdown-it-synapse/src"],
      //  "markdown-it-synapse/*": ["../markdown-it-synapse/src/*"],
      //  "markdown-it-synapse-table": ["../markdown-it-synapse-table/src"],
      //  "markdown-it-synapse-table/*": ["../markdown-it-synapse-table/src/*"],
      "vite-config": ["../vite-config/src/index.ts"],
      "vite-config/*": ["../vite-config/src/*"]
    }
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "src/**/*.js",
    "src/**/*.json",
    "src/**/*.cjs"
  ],
  "references": [
    //    {
    //      "path": "../synapse-client"
    //    },
    //    {
    //      "path": "../synapse-types"
    //    },
    //    {
    //      "path": "../markdown-it-synapse"
    //    },
    //    {
    //      "path": "../markdown-it-synapse-table"
    //    },
    { "path": "../vite-config" }
  ]
}
