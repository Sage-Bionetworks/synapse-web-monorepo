import App from '@sage-bionetworks/synapse-portal-framework/App'

function RootApp() {
  return (
    <App>
      {/* Add any components that must be shown on every page in the app */}
    </App>
  )
}

export default RootApp
