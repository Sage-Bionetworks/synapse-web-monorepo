import { styled } from '@mui/material/styles'
import { Box } from '@mui/material'

const DetailsPageLayout = styled(Box)({
  display: 'flex',
  paddingTop: '50px',
  gap: '50px',
})

export default DetailsPageLayout
